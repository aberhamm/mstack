---
id: 084
title: extract the Step 7a completion sequence to complete.sh
status: skipped
blocked-by: [069, 078, 085]
priority: 48
goal: audit-remediation-roadmap
allows-migrations: false
needs-review: eng
review-required: eng
created: 2026-07-30
qa: automated
reviews:
  - type=eng verdict=approved date=2026-07-30 by=mstack-plan-doctor
skipped: 2026-07-31
skipped-reason: "its own amendment argued it out: 085 recovers the context budget editorially, 078 (a dependency) is now dropped, leaving a ~90-line zero-behavior-change refactor of the riskiest path"
---

## Requirements

`skills/mstack-run/SKILL.md` Step 7a ("### 7a. On success", lines 786-1075,
~290 lines) narrates a FIXED linear sequence of gate assertions and git
commands in prose — the audit's context-budget finding, and prose-narrated
determinism is exactly the pattern this repo keeps converting to scripts
(`result-gate.sh`, `review-gate.sh` precedents). The "Full linear order"
block (SKILL.md:788-798) already states the spine as an ordered list; extract
it into a deterministic script so the orchestrator calls one command and
branches on exit codes instead of re-narrating ~290 lines every completion.

**Acceptance criteria**

- [ ] New `skills/mstack-run/scripts/complete.sh` executes the spine:
      `result-gate.sh assert-health-result` → `assert-completable` →
      `assert-no-downgrade` → frontmatter status write (in-progress→done per
      plan 069's fix) + `completed:`/`reviewed:`/`qa:` stamps → append
      Implementation Notes from the `--notes` input (Full linear order
      step 4 — the spine executes the append; the CONTENT stays
      LLM-composed per the judgment-writes criterion below) → completion
      commit (MODIFIED+CREATED staged, DELETED via `git add -A --`) →
      hash-backfill `git commit --amend` → `assert-work-committed` → archive
      `git mv` + commit → annotated tag `git tag -a`.
- [ ] BEHAVIOR-IDENTICAL: no semantic change to any gate, the Full linear
      order, commit message format (`type(scope): subject` +
      `Refs: docs/plans/<file>` trailer), archive commit message
      (`chore: archive plan NNN (done)`), tag name and annotated-ness
      (`-a -m`, the `--follow-tags` pairing), or any abort path (gate-open →
      blocked + `needs-review`, health-invalid → failed +
      `failed-reason: health-gate-unavailable`, work-uncommitted → HALT, no
      auto-`git add`).
- [ ] Exit codes reuse the existing taxonomy — 23 (not completable), 24
      (downgrade), 26 (hook missing), 28 (work uncommitted), 30 (health
      result invalid) — defined in `lib.sh`; new codes only if unavoidable
      and non-colliding (pick-next 10-19, seam 20, plan-ref 21-22, 25/27/31/32
      taken).
- [ ] LLM-judgment writes STAY in prose: Implementation Notes composition and
      the commit message content are composed by the skill and passed to
      `complete.sh` as inputs (file/heredoc), not generated by it.
- [ ] SKILL.md Step 7a shrinks to: compose the judgment content, call
      `complete.sh`, branch on its exit codes.
- [ ] A completion smoke pins the invariants: happy path, each gate-abort
      path (health-invalid, gate-open, downgrade, work-uncommitted), the
      amend backfill, the archive move, and the annotated tag.
- [ ] Scoped-run manifest handling is not dropped: Step 7a also performs the
      scoped-run manifest update/cleanup interaction (SKILL.md ~1044-1053 and
      the Step 7c2 region); the extraction must either include it in
      `complete.sh` or explicitly leave it in prose — and state which.
- [ ] Push guidance stays in prose: the do-not-push guidance and the
      `git push --follow-tags` user instruction that Step 7a/Step 11 text
      carries remain skill prose; `complete.sh` never pushes — state that
      explicitly.

## Design

`complete.sh <plan-path> --result <result-block-file> --notes <notes-file>
--message <commit-msg-file>` (exact interface decided at implementation;
inputs are files, not args, to survive quoting). It sources `lib.sh`, calls
the existing gate scripts rather than reimplementing them, and prints one
machine line per stage so the skill can report progress. Abort paths that
write plan frontmatter (blocked + `needs-review:` from assert-completable
stderr; failed + `failed-reason`) move into the script too — they are
deterministic edits with fixed commit messages (`chore(plan NNN): blocked,
review gate open`).

Smoke: a new `complete-smoke.sh` rather than extending `hook-chain-smoke.sh`
— the hook suite tests the write-layer hook, this tests the honest path;
mixing them muddies the layer model. Fixture repo pattern copied from
`review-gate-smoke.sh` (temp git repo, fixture plan, stub result blocks).
Register the suite in AGENTS.md's smoke list, the pre-commit hook source
(`skills/mstack-run/hooks/pre-commit:62` suite loop), and the `.githooks/`
copy (edit the source, `cp` to `.githooks/` — never `./setup` from
`~/dev/mstack`).

Blocked-by rationale: 069/070 touch Step 7a's text/semantics directly (069's
in-progress→done status fix in particular changes the status-write line this
script hard-codes); 063/067 add smoke suites to the SAME pre-commit
suite-loop line this plan also edits (`hooks/pre-commit:62`), so landing
after them avoids re-doing the loop registration. Extracting before any of
the four would force that work to be re-done inside `complete.sh` or the
hook.

Known open follow-up, noted but NOT changed: some pre-fix tags are
lightweight and `--follow-tags` doesn't push them (the tag-stranding story,
SKILL.md:1026-1037 and 1061-1074). Keep current behavior.

Testing approach: unit-only.

**Files expected to change:**

- `skills/mstack-run/scripts/complete.sh`: NEW (100755 +
  `git update-index --chmod=+x`).
- `skills/mstack-run/scripts/complete-smoke.sh`: NEW (100755, same).
- `skills/mstack-run/SKILL.md`: Step 7a collapsed to compose → call → branch.
- `skills/mstack-run/hooks/pre-commit` + `.githooks/pre-commit`: add
  complete-smoke to the suite loop.
- `AGENTS.md`: register the new suite in Development Commands.

**Out of scope:** any redesign of gate semantics, ordering, messages, or tag
shape; fixing the lightweight-tag stranding; Step 7b (failure path) beyond
what the health-invalid abort already shares; changing `result-gate.sh` /
`review-gate.sh`; the picker.

## Tasks

1. Enumerate the invariants from current Step 7a text (order, messages, tag,
   abort behaviors) into the smoke's assertion list FIRST — the extraction is
   judged against this list.
2. Write `complete.sh`: input files, stage sequence, existing-gate
   delegation, abort-path frontmatter writes, per-stage output lines.
3. Write `complete-smoke.sh` covering happy path + the four gate aborts +
   amend + archive + annotated-tag assertions; both scripts 100755.
4. Rewrite SKILL.md Step 7a to compose-call-branch, preserving the layered
   enforcement prose (anti-forgetfulness note, fixture exemption) as short
   pointers.
5. Register the suite in AGENTS.md and the pre-commit hook source; `cp` to
   `.githooks/pre-commit`.
6. Run all smoke suites + shellcheck.

## Verification

Checks:

- [cmd] `test -x skills/mstack-run/scripts/complete.sh && test -x skills/mstack-run/scripts/complete-smoke.sh`
- [cmd] `bash skills/mstack-run/scripts/complete-smoke.sh`
- [cmd] `shellcheck skills/mstack-run/scripts/complete.sh skills/mstack-run/scripts/complete-smoke.sh`
- [cmd] `grep -q 'complete-smoke' skills/mstack-run/hooks/pre-commit && grep -q 'complete-smoke' .githooks/pre-commit`
- [cmd] `grep -q 'complete-smoke' AGENTS.md`
- [assert] `git tag -a` remains: `grep -c 'git tag -a' skills/mstack-run/scripts/complete.sh` → >= 1
- [cmd] `bash skills/mstack-run/scripts/script-mode-smoke.sh`
- [cmd] `bash skills/mstack-run/scripts/review-gate-smoke.sh`
- [cmd] `bash skills/mstack-run/scripts/hook-chain-smoke.sh`

## Backlog amendment (2026-07-31)

SPLIT AND DEFERRED. The characterization test half is now plan 086
(`complete-smoke.sh` against current behavior) and should land early and
independently. This plan retains ONLY the extraction of the Step 7a sequence
into `complete.sh`, and now depends on 085 running first.

Rationale for the deferral: of the ~290 lines cited as the context-budget
argument, roughly 200 are rationale essays and a tag-stranding war story told
twice (`SKILL.md:1030-1038` and `:1061-1074`). Plan 085 relocates those
editorially at zero risk, leaving ~90 lines of ordered procedure. Extracting
90 smoke-pinned lines is a far smaller and more obviously correct job than
extracting 290 unpinned ones.

Dependencies corrected from six to three. 063 and 067 collided with this plan
on exactly one line (`hooks/pre-commit:62`, the suite loop) — a two-token
merge, not a dependency. 070 never touches Step 7a. Only 069 (status
transition, rollback, linear-order renumbering) and 078 (adds a new code-gate
branch INSIDE Step 7a) are genuine semantic prerequisites; 085 is added
because it shrinks the extraction target.
